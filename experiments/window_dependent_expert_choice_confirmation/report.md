Final classification: INSUFFICIENT_UNTOUCHED_DATASETS

PRIMARY UNTOUCHED DATASETS: (none)
CONTAMINATED / EXCLUDED DATASETS: BeijingAirQuality, ETTh1, ETTh2, ETTm1, ETTm2, Electricity, ExchangeRate, Traffic, Weather

```text
TEST SET ACCESSED: NO
TEST CACHE LOADED: NO
TEST METRICS COMPUTED: NO
```

# Window-Dependent Expert Choice: First Clean New-Dataset Confirmation

`CANONICAL_METHOD_FOUND: YES` (source: `experiments/window_dependent_expert_choice_hv/run_window_dependent_expert_choice_hv.py`, sha256 `3a7bd2ed3c8f6cb9...`).

## Step 1: contamination ledger

Full local BasicTS catalog: `22` datasets. Contaminated (already influenced this research program): `9` -- BeijingAirQuality, ETTh1, ETTh2, ETTm1, ETTm2, Electricity, ExchangeRate, Traffic, Weather.

The seed list matched local evidence exactly for these 9 datasets: ExchangeRate, Traffic, BeijingAirQuality, and ETTm2 were independently confirmed as a separate 'generalization' family (experiments/behavioral_competence/generalization/dataset_selection.json, experiments/behavioral_competence/timefuse_probe/) used for a prior LearnedProbe-Rank generalization study on 2026-08-20 and a TimeFuse probe study; no additional contamination was found beyond the seed set.

## Step 2: static eligibility (performance-blind)

Candidate untouched datasets considered: `13` -- CA, GBA, GLA, Gaussian, Illness, METR-LA, PEMS-BAY, PEMS03, PEMS04, PEMS07, PEMS08, Pulse, SD.

| Dataset | Has graph | Rescale | Null val | Metrics | Timestamps | Est. confirmation windows | Eligible | Reason if not |
|---|---|---|---|---|---:|---:|---|---|
| CA | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 35040 | 6901 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| GBA | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 35040 | 6901 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| GLA | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 35040 | 6901 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| Gaussian | False | True | nan | [] | 10000 | 1893 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): rescale=true, metrics=[] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| Illness | False | False | nan | ['MAE', 'MSE'] | 966 | 86 | False | confirmation_eval (validation block, 60-80%) would only contain 86 windows at input_len=96/horizon=12, below the static pre-registered floor of 400 (chosen from the smallest development dataset's own confirmation-equivalent window count, ETTh2 router_val=613, before any candidate's size was computed). Too thin for the predeclared block-24 bootstrap and per-fold scorer training to be statistically meaningful. |
| METR-LA | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 34272 | 6747 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| PEMS-BAY | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 52116 | 10316 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| PEMS03 | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 26208 | 5135 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| PEMS04 | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 16992 | 3291 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| PEMS07 | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 28224 | 5538 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| PEMS08 | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 17856 | 3464 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| Pulse | False | True | nan | ['MAE', 'RMSE', 'MAPE'] | 20000 | 3893 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): rescale=true, metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |
| SD | True | True | 0.0 | ['MAE', 'RMSE', 'MAPE'] | 35040 | 6901 | False | regular_settings convention differs from every development dataset (has_graph=false, rescale=false, null_val=NaN, metrics=[MAE,MSE], norm_each_channel=true): has_graph=true, rescale=true, null_val=0.0 (not NaN), metrics=['MAE', 'RMSE', 'MAPE'] (not MAE/MSE convention), norm_each_channel=false. Adapting this dataset would require real preprocessing/masking/evaluation-convention changes to the existing generic pipeline functions (build_histories_targets' isfinite-based masking assumes NaN-missing, not null_val=0.0; sample_mae/std normalization assumes rescale=false) -- i.e. dataset-specific method tuning, which is disallowed for eligibility. |

Eligible untouched datasets: `0` (required >= `4`).

0 eligible untouched dataset(s) found; used all of them (<= 6).

## Result: STOP -- INSUFFICIENT_UNTOUCHED_DATASETS

Every dataset in the local BasicTS installation falls into one of three buckets:

1. **Already contaminated** (9 datasets: ETTh1, ETTh2, ETTm1, Weather, Electricity, ExchangeRate, Traffic, BeijingAirQuality, ETTm2) -- either directly used to develop/validate the Expert-Choice method family, or used in the separate prior LearnedProbe-Rank 'generalization' study and TimeFuse probe study on the same 4 additional datasets.
2. **Format-incompatible without real method changes** (graph-structured traffic datasets: PEMS03/04/07/08, METR-LA, PEMS-BAY, CA, GBA, GLA, SD -- all use `has_graph=true`, `rescale=true`, `null_val=0.0`, and `metrics=[MAE,RMSE,MAPE]`, a fundamentally different preprocessing/masking/evaluation convention than every dataset this method has ever run on; adapting them would require real code changes to masking and normalization, i.e. dataset-specific method tuning, which eligibility explicitly disallows), plus `Gaussian`/`Pulse` (synthetic, single-variable, `rescale=true`) and `UEA` (a classification benchmark collection with no forecasting train/val/test array).
3. **Format-compatible but statistically too thin** (`Illness`: matches the development convention exactly -- `has_graph=false`, `rescale=false`, `null_val=NaN`, `norm_each_channel=true`, `metrics=[MAE,MSE]` -- but only 966 total timestamps yields an estimated 86 confirmation_eval windows, far below the 400-window static floor and less than 15% of the smallest development dataset's own confirmation-window count (ETTh2, 613 windows).

Independently of the above, **no untouched dataset currently has trained frozen expert checkpoints or a built walk-forward cache**: `checkpoints/costarts_walkforward_{Illness,PEMS03,...}` and the corresponding `cache/` directories simply do not exist. Even if a candidate passed the static eligibility bar, running confirmation would first require training 5 new frozen forecasting experts (DLinear, PatchTST, iTransformer, TimesNet, ModernTCN) across 3 chronological walk-forward stages -- a substantial new training investment, not a single frozen-method evaluation pass. This is a second, independent reason confirmation cannot proceed today without the user acquiring/approving a genuinely new, compatible, adequately-sized dataset.

**No core selection, causal OOF, or confirmation_eval was run. No dataset-specific code was written or tuned to try to manufacture eligibility. This is being reported as a negative/blocked outcome, not rescued.**

## Recommendation

Confirmation cannot proceed with the current local BasicTS installation. Two honest paths forward:

1. Acquire or point this repository at a genuinely new, walk-forward-compatible, adequately-sized dataset outside the current BasicTS bundle (same `has_graph=false`/`rescale=false`/`null_val=NaN`/`metrics=[MAE,MSE]` convention, several thousand+ timestamps), then rerun this exact script -- the contamination ledger and eligibility logic require no changes.
2. If graph-structured traffic datasets (PEMS/METR-LA/PEMS-BAY/CA/GBA/GLA/SD) are acceptable confirmation targets, that requires a deliberate, separately-scoped methodological adaptation (real `null_val=0.0` masking and `rescale=true` evaluation support) before any confirmation run -- not a quiet relaxation of today's eligibility bar.
